<div class="container" style="margin-top:15vh">
  <h2 style="padding-top:100px;text-align:center;margin-bottom:30px;"> Toutes les catégories </h2>

  <div class="row d-flex justify-content-around mb-5">
    <% @categories.each_with_index do |category, index| %>
        <div class="my-4 mx-2" style="background-image: url('<%= cl_image_path category.photos.first.key %>');background-size: cover;border-radius:3px;height:180px;width:360px;">
          <h3 class="catvignettename"><%= category.name %></h3>
        </div>
      <%end%>
  </div>
</div>
