<div style="margin-top:35vh;margin-bottom:15vh;width:100%">
  <div class="fildariane">
    <%= link_to category_path(@subcategory.category) do  %>
      <span> <%=  @subcategory.category.name.capitalize() %> </span>
    <% end %>
    >
    <%=link_to  category_subcategory_path(@subcategory.category, @subcategory) do  %>
      <span> <%=  @subcategory.name %> </span>
    <% end %>
  </div>
  <div style="display:flex;justify-content:center">
    <h2 class="mb-3">Tous nos produits « <%=@subcategory.name%> »</h2>
  </div>
    <div style="display:flex;justify-content:center;width:100%;height:50px;"><%== pagy_bootstrap_nav(@pagy) %>
    </div>
    <p><i><%== pagy_info(@pagy) %></i></p>
    <%# Oeuvres: %>
    <%#= @products.count%>
    <%# / %>
    <%#= @subcategory.products.where(main: true).count%>

  <div style="flex-wrap:wrap;display:flex;justify-content:center" data-controller="favorites">
    <% @products.each do |product| %>
      <%= render "shared/#{Shop.first.card}", product: product%>
    <%end%>
  </div>
    <div style="display:flex;justify-content:center;width:100%;height:50px;"><%== pagy_bootstrap_nav(@pagy) %>
    </div>
</div>
