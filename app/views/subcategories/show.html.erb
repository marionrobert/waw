<div class="container" style="margin-top:35vh;margin-bottom:15vh;">
  <h2 class="mb-3">Tous nos produits « <%=@subcategory.name%> »</h2>
  <div class="row d-flex justify-content-center">
    <% @products.each do |product| %>
      <%= render "shared/#{Shop.first.card}", product: product%>
    <%end%>
  </div>
</div>
